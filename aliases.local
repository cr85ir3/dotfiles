# google search within shell
ggx() {·
  lynx -accept_all_cookies https://www.google.com/search?q=${(j:+:)*}
}

# find aliases containing word
alias alias?='alias | grep'

# use 'hub' with 'git hub'
eval "$(hub alias -s)"
